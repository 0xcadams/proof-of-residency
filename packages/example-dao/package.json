{
  "name": "@proof-of-residency/example-dao",
  "version": "0.0.1",
  "description": "A demo project to illustrate how to integrate with the Proof of Residency contract.",
  "private": true,
  "license": "MIT",
  "scripts": {
    "analyze": "yarn lint && slither . --filter-paths \"@openzeppelin\"",
    "build": "yarn clean && yarn compile",
    "clean": "yarn hardhat clean",
    "compile": "yarn hardhat compile",
    "coverage": "yarn build && yarn hardhat coverage --temp artifacts",
    "lint": "solhint 'contracts/**/*.sol' && prettier --list-different 'contracts/**/*.sol'",
    "size": "yarn hardhat size-contracts",
    "start": "hardhat node",
    "test": "yarn hardhat test"
  },
  "dependencies": {
    "@proof-of-residency/contracts": "file:../contracts"
  }
}
