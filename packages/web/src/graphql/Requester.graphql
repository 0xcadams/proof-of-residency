query GetRequesterById($id: ID!) {
  requester(id: $id) {
    id
    numCommitments
    commitments {
      ...CommitmentFields
    }
    numTokens
    tokens {
      id
    }
    numTokenChallenges
    tokenChallenge {
      id
    }
  }
}
