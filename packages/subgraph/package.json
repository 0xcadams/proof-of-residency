{
  "name": "@proof-of-residency/subgraph",
  "version": "1.0.2-alpha.0",
  "description": "The subgraph for Proof of Residency.",
  "license": "MIT",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git@github.com:proof-of-residency/proof-of-residency.git"
  },
  "scripts": {
    "build": "graph build",
    "codegen": "graph codegen",
    "start": "docker-compose up --force-recreate",
    "deploy:arbitrum-one": "yarn prepare:arbitrum-one && graph deploy --product hosted-service proof-of-residency/proof-of-residency",
    "create:local": "yarn prepare:local && graph create --node http://localhost:8020/ proof-of-residency/proof-of-residency",
    "remove:local": "graph remove --node http://localhost:8020/ proof-of-residency/proof-of-residency",
    "deploy:local": "yarn prepare:local && yarn create:local && graph deploy --node http://localhost:8020/ --ipfs http://localhost:5002 proof-of-residency/proof-of-residency",
    "prepare:local": "mustache config/hardhat.json subgraph.mustache > subgraph.yaml",
    "prepare:arbitrum-one": "mustache config/arbitrum-one.json subgraph.mustache > subgraph.yaml"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.24.1"
  },
  "devDependencies": {
    "mustache": "^4.2.0"
  }
}
